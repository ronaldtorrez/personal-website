import { projectCollection } from '@content/projects/config.ts'
import { logoCollection } from '@assets/logos/config.ts'

export const collections = {
	projects: projectCollection,
	logos: logoCollection
}
